
---
- hosts: CSR-Prisma-Hub
  gather_facts: no

  tasks:
  - name: delete vxlan neighbor vni 4096
    ios_config:
     replace: block
     lines:
      - no ingress-replication {{ ip }}
     parents:
      - interface nve1
      - member vni 4096
      
  - name: delete vxlan neighbor vni 4097
    ios_config:
     replace: block
     lines:
      - no ingress-replication {{ ip }}
     parents:
      - interface nve1
      - member vni 4097

  - name: delete vxlan neighbor vni 4098
    ios_config:
     replace: block
     lines:
      - no ingress-replication {{ ip }}
     parents:
      - interface nve1
      - member vni 4098
      
  - name: delete vxlan neighbor vni 4099
    ios_config:
     replace: block
     lines:
      - no ingress-replication {{ ip }}
     parents:
      - interface nve1
      - member vni 4099   
